import styled from "styled-components";

export const NavWrapper = styled.div(
    ({}) => {
        return{
            //Mirabel, enter style for nav here
            display: "flex",
        }
    }
)